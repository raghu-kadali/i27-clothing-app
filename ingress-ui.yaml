apiVersion: networking.k8s.io/v1      
kind: Ingress
metadata:
  name: i27-clothing-ingress    
  namespace: cart-dev-ns
spec:
  rules:
  - host: dev-clothing.raghuprojectdevops.in
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: i27-clothing-dev
            port:
              number: 80
              